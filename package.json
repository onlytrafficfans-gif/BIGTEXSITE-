{
  "name": "texx-social",
  "version": "1.0.0",
  "private": true,
  "description": "TEXX SOCIAL — static frontend + Supabase backend (migrations, RLS, Edge Functions).",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "lint": "node scripts/lint-migrations.js",
    "lint:sql": "node scripts/lint-migrations.js",
    "typecheck": "deno check supabase/functions/delete-account/index.ts",
    "test": "node scripts/run-sql-tests.js",
    "build": "node scripts/build-check.js"
  },
  "engines": {
    "node": ">=18"
  }
}
